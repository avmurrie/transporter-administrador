import { FiltroservicioPipe } from './filtroservicio.pipe';

describe('FiltroservicioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroservicioPipe();
    expect(pipe).toBeTruthy();
  });
});

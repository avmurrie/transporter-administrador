import { FiltrotarifaPipe } from './filtrotarifa.pipe';

describe('FiltrotarifaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrotarifaPipe();
    expect(pipe).toBeTruthy();
  });
});
